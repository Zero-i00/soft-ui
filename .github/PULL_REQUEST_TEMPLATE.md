## Summary

<!-- What does this PR do? Why? -->

## Checklist

- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes
- [ ] Changeset added (`pnpm changeset`) — required if `primitives` or CLI changed
- [ ] Documentation updated — required if a component was added or changed
