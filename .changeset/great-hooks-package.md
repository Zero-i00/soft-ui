---
'@soft-ui/hooks': minor
---

Новый пакет `@soft-ui/hooks`: хук `useTheme` (useSyncExternalStore), синглтон `themeService` (getTheme/getResolved/setTheme/toggleTheme/subscribe + readonly STORAGE_KEY/ATTRIBUTE/PREFER) и anti-FOUC `themeScript`, переехавшие из registry.
