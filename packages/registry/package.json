{
	"name": "@soft-stack/registry",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "tsx scripts/build-registry.ts",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest",
		"clean": "rm -rf .turbo"
	},
	"devDependencies": {
		"tsx": "^4.19.0",
		"typescript": "^5.6.0",
		"vitest": "^2.1.0",
		"glob": "^11.0.0"
	}
}
